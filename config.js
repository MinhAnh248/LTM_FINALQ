// Cấu hình API URL
const API_CONFIG = {
    // Thay đổi URL này sau khi deploy backend lên Render
    BASE_URL: 'https://expense-tracker-backend-abc123.onrender.com/api'  // Thay bằng URL thực tế
};

// Export cho sử dụng
window.API_CONFIG = API_CONFIG;