// Cấu hình API URL
const API_CONFIG = {
    // Thay đổi URL này sau khi deploy backend lên Render
    BASE_URL: 'https://your-backend.vercel.app/api'  // Thay bằng URL Vercel backend
};

// Export cho sử dụng
window.API_CONFIG = API_CONFIG;